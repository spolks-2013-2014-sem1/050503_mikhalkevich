#Task 8. Parallel TCP with processes + serial UDP. Send file - from server to client:
#Parametrs in command line (server):
#Mode 1: ip port {u|t}
#Mode 2: port {u|t}       (ip can be any)
#Parallel processing clients with TCP, serial with UDP
#For closing server - Ctrl+C

#Parametrs in command line (client):
#ip port filename {u|t}

